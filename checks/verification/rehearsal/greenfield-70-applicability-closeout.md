# Greenfield #70 applicability closeout (U4-2)

Owner: **#70**. Status: tip AC→evidence map + Quiet Receipt finding re-score.
Main pin at land: `d00ebd80066d75cd13998efe963743d15b8316ff` / `b2c-app-builder@0.221.25` → stamp **0.221.26**.
Typed module: `catalog/providers/greenfield-70-applicability-closeout-map.ts`.
Fixtures: `checks/verification/fixtures/greenfield-70-applicability-closeout.fixtures.ts`
  + consume `checks/verification/fixtures/design-surface-applicability.fixtures.ts`.

This slice is **surface-technique / applicability residual closeout** only. It
does **not** run a live greenfield benchmark, publish-of-evidence, redo the #66
umbrella, reopen #108, steal #403 ladder/preview/delivery, or implement
siblings #71/#72/#73/#75/#76.

## #403 boundary (coordinate only)

Quality bar is **retained** when a technique is N/A (consume PR #470 growth
producer wording + `technique exclusions retain the quality bar` fixture).
**#70 does not own** the continuous 11-star ladder, preview path, or delivery
path — those stay on **#403 OPEN**. Not-applicable motion/CRO is not permission
to ship careless static pages; neither does #70 invent a universal score,
style, provider, or full ladder inside this issue.

## Consumed tip surfaces (do not rebuild)

| Area | Path | Note |
| --- | --- | --- |
| Applicability ontology | `catalog/ontology/design-surface-applicability.ts` | job ⊥ interaction; purpose prose not authority (#108 CLOSED) |
| Page gates | `checks/validation/business/design/surface-page-gates.ts` | static ≠ conversion ≠ scroll-linked |
| DSA fixtures | `design-surface-applicability.fixtures.ts` (29 checks) | matrix + invented-scrollytelling + quality bar + residual guidance |
| Quiet Receipt | `examples/mixed-surfaces/` | privacy / conversion / cinematic |
| Finding artifact | `…/MIXED_SURFACE_INDEPENDENT_REVIEW.md` | re-scored this slice |
| Growth quality bar | `catalog/workflows/growth-revenue.ts` (+ #470) | technique N/A ≠ skip craft |
| #66 umbrella | `adapters/greenfield/*`, `greenfield-delivery-audit-*` | **consume only** |

## Verification / acceptance → evidence

| #70 acceptance row | Tip evidence | Fixture / test | Status |
| --- | --- | --- | --- |
| Each matrix row has a fixture or reviewed applicability test | ontology + page gates + Quiet Receipt | full `design-surface-applicability` suite | **done** |
| Static pages not forced to invent animations or conversion goals | page gates; landing producer gates | `page-gates: static privacy without invented CRO passes`; `static_document_invented_scrollytelling fails…`; residual guidance pins | **done** |
| Selected/implemented rich interactions cannot bypass obligations | ontology + page gates | selected scrollytelling / implemented hooks / cinematic conversion CRO fail / mixed live | **done** |
| Provider availability never silently changes selection or authorizes spend | unavailable hold + distilled-recipe refuse | `bespoke motion with a blocked 60fps route is unavailable`; residual guidance distilled-recipe refuse | **done** |
| Producer instructions, reference delivery, deterministic checks, and independent rubric agree | growth producer + rubric + finding artifact | residual guidance pins; finding re-score this slice | **done** (finding reconciled) |
| Approved scope changes invalidate only affected acceptance | ONB-08 consult reopen | live ONB-08 consult reopen fixture (landed #120) | **done** |
| PR includes before/after obligation map, test evidence, any policy decision | this doc + typed map + PR body | `greenfield-70-applicability-closeout` fixtures | **done** (this PR) |

## Working applicability matrix → evidence

| Matrix surface/condition | Tip path | Fixture name(s) | Status |
| --- | --- | --- | --- |
| Static legal/support page | ontology + `surface-page-gates.ts` + `privacy.html` | static legal page; static privacy passes; invented-scrollytelling fails; mixed live | **done** |
| Conversion landing without scroll-linked behavior | ontology + `conversion.html` | conversion without 60fps; conversion without CRO fails; mixed live | **done** |
| Selected or implemented scrollytelling | ontology + `cinematic.html` | selected scrollytelling requires 60fps; implemented hooks; cinematic conversion CRO; mixed live | **done** |
| Native UI using standard transitions | ontology | native standard transitions do not select paid motion catalog | **done** |
| Bespoke motion | ontology | bespoke motion with blocked 60fps → unavailable | **done** |
| Explicitly required reference provider unavailable | ontology + `design-evidence-stack.md` | unavailable hold; residual guidance refuses distilled-recipe fallback | **done** |

## Finding-artifact reconcile (E2)

| Prior leftover (2026-09-09 Verdict pass) | Tip HTML (`8a49788`+) | Re-score 2026-09-19 |
| --- | --- | --- |
| Privacy copy drift (“stores” vs conversion “did not store”) | privacy: “does not store the email you submit” | **cleared** |
| Nav links ~19px (below 24px) | all three pages: `min-height` / `min-width: 24px` | **cleared** |
| Cinematic 48px plate/figcaption overlap at 1280 | active labels hidden; stage `min-height: 16.5rem`; figcaption paper/`z-index` | **cleared** |

Method: file-based independent re-score of tip sources (fixture-tier; browser
automation unavailable). Documented in the finding-file header. **Not** #72 live.

## Hard holds (this slice)

- Live greenfield complete-business benchmark / publish-of-evidence → **#72**
- Sibling implementations → **#71 / #73 / #75 / #76 / #72** (separate deepens)
- Redo #66 umbrella framework
- Reopen #108 purpose-vs-interaction
- Steal #403 11-star ladder / preview / delivery
- U5 / #511, Formation, App Review, prices, credentials, paid-tool install, silent spend fallback
- New design system / blanket audit deletion / second applicability store / infer technique from purpose prose
- Claiming complete-business from Quiet Receipt fixture-tier alone

## U4 remaining sequence (after #70)

```
#71 → #73 → #75 → #76 → #72 last
```

- Skip **#74 CLOSED**
- **No U5 / #511** until HoE orders
- After #70 close: **STOP → #71 deepen separate**

## Next

Parent Shepherd: merge PR when CI green → handoff comment → **close #70** →
**STOP — next deepen is #71 ONLY** (separate). Do not implement siblings here.
