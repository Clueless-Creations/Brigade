import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";import{importReferenceProductProfile}from"./reference-product-profile.js";const fixture=JSON.parse(readFileSync("docs/contracts/product-profile/examples/observed-reference.json","utf8"));test("valid observed-reference remains revision-bound and read-only",()=>{const r=importReferenceProductProfile(fixture);assert.equal(r.revision,fixture.revision);assert.equal(r.profile.mode,"observed-reference")});test("intended profile cannot enter reference registry",()=>{const p={...fixture,mode:"intended"};assert.throws(()=>importReferenceProductProfile(p),/reference_profile/)});