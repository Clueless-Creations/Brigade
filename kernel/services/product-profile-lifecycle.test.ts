import assert from"node:assert/strict";import{cpSync,mkdtempSync,rmSync}from"node:fs";import os from"node:os";import path from"node:path";import test from"node:test";import{refreshIntendedProductProfile}from"./intended-product-profile.js";import{productProfileLifecycleStatus}from"./product-profile-lifecycle.js";test("status is passive and surfaces intended freshness",()=>{const r=mkdtempSync(path.join(os.tmpdir(),"pp-life-"));try{cpSync(path.resolve("examples/tuck"),r,{recursive:true});assert.equal(productProfileLifecycleStatus(r).action,"refresh-intended");refreshIntendedProductProfile(r,"2026-09-20T23:10:00Z");assert.equal(productProfileLifecycleStatus(r).action,"query")}finally{rmSync(r,{recursive:true,force:true})}});