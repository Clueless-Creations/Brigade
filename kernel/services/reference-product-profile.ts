import { validateProfile } from "../../contracts/product-profile/conformance.mjs";export function importReferenceProductProfile(profile:any){const errors=validateProfile(profile);if(errors.length)throw new Error("reference_profile.invalid:"+errors.join("|"));if(profile.mode!=="observed-reference")throw new Error("reference_profile.mode_required");return Object.freeze({schemaVersion:1,id:profile.id,revision:profile.revision,productId:profile.product.id,productVersionId:profile.product.version.id,profile});}