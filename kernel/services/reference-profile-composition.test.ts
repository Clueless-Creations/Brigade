import assert from"node:assert/strict";import test from"node:test";import{composeReferenceCandidates}from"./reference-profile-composition.js";test("composition is deterministic proposal-only and traceable",()=>{const r=composeReferenceCandidates({brief:"progression for cooking",candidates:[{sourceProfileId:"profile:duo",sourceRevision:"sha256:x",sourceRecordIds:["system:progress"],targetConstraintIds:["constraint:offline"],decision:"adapt",rationale:"Preserve structure without source branding."}]});assert.equal(r.authority,"proposal-only");assert.deepEqual(r.acceptedWrites,[]);assert.equal(r.candidates[0].sourceRecordIds[0],"system:progress")});